START
    initialize an integer variable blockType with the value of the integer array world[][] with player's coordinates as indexes
    if blockType is different from a constant integer AIR
        add blockType to inventory list
        change the value of the array world[][] with player's coordinates as indexes to AIR
        say in new line "Mined " (name of the block returned by getBlockName method with blockType as argument) "."
    else
        say in new line "No block to mine here."
    wait for player to press enter with waitForEnder method
END