mineBlock()
BEGIN
    GET blockType in player position
    IF blockType is not AIR
        ADD blockType to inventory
        REPLACE blockType with AIR
        PRINT "Mined" + blockType + "."
    ELSE
        PRINT "No block to mine here."
    ENDIF
    WAIT for enter
END
