function initGame(integer worldWidth, integer worldHeight):

game.worldWidth = worldWidth

game.WorldHeight = worldHeight

game.world = new int array [worldWidth][worldHeight]

playerXcoord = worldWidth / 2
playerYcoord = worldHeight / 2

inventory = new ArrayList

end