Take 2 parameters: int item, int count

int removedCount = 0
iterator through craftedItems

WHILE iterator.hasNext() == true:
	int i = iteratior.next()
	IF i == item
		iterator.remove()
		removedCount++
		IF removedCount == count
			break
